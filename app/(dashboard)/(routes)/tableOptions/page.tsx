import { DataTableOption } from "@/components/tableOptions/app.table"

export const metadata = {
    title: 'Loại Bàn',
    description: ':List Table Options By NamJS',
}
const TableOptions = () => {
    return(
        <DataTableOption/>
    )
}
export default TableOptions